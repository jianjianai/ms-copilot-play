{
    "installCommand": "pnpm install",
    "buildCommand": "pnpm run build-vercel",
    "outputDirectory": "./dist",
    "routes": [
        {
            "src": "/.*",
            "dest": "/api/server"
        }
    ]
}